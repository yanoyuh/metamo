import React from 'react'
import ReactDOM from 'react-dom/client'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <div className="min-h-screen bg-base-100">
      <h1 className="text-4xl font-bold text-center pt-20">Metamo</h1>
      <p className="text-center mt-4">AI画像編集Webアプリケーション</p>
    </div>
  </React.StrictMode>,
)
